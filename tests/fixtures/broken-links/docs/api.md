# API Reference

## Endpoints

### GET /api/health

Returns the health status of the service.

### POST /api/data

Submit data for processing.

**Request body:**

```json
{
  "name": "example",
  "value": 42
}
```
